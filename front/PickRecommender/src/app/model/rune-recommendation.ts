import { Rune } from "./rune";

export class RuneRecommendation {
    constructor(public primaryRunesPathName, public secondaryRunesPathName,
                public primaryRunes, public secondaryRunes) { }
}